export const validate = values => {
  const errors = {}
  /*if (!values.phone) {
    errors.phone = 'Required'
  } else if (values.phone.length !== 10 || /[0-9]$/i.test(values.phone) !== true) {
    errors.phone = 'Must be exactly 10 digits'
  }*/

  return errors
}