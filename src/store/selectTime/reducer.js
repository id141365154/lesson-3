import * as types from './types'

/*const initialState = {
  hours: '00',
  minutes: '00',
}

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    case types.OPEN_SELECT_TIME:{
      console.log(state, action)
      return {
        ...state,
        value: action.payload.value,
      }
    }
    default:
      return state
  }
}*/
